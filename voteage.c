#include <stdio.h>

int main() {
    int myage = 18;
    int voteage = 18;
    if (myage >= voteage) {
        printf("Old enough to vote\n");
    } else {
        printf("Not old enough to vote\n");
    }
}
